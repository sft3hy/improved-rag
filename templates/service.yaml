apiVersion: v1
kind: Service
metadata:
  name: {{ include "improved-rag-frontend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
  selector:
    app: {{ include "improved-rag-frontend.name" . }}
