apiVersion: v1
kind: Service
metadata:
  name: {{ include "improved-rag-frontend.fullname" . }}
  labels:
    {{- include "improved-rag-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "improved-rag-frontend.selectorLabels" . | nindent 4 }}